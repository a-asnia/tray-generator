// ── монтирование ──
import React from "react";
import ReactDOM from "react-dom/client";
import TrayGenerator from "./App.jsx";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(React.createElement(TrayGenerator));
