# Публикация tray-generator.html на GitHub Pages.
# Страница обновляется сама при каждом пуше собранного файла.
name: pages

on:
  push:
    branches:
      - claude/tray-generator-modular-vite-531gqd
      - main
    paths:
      - tray-generator.html
      - .github/workflows/pages.yml
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - name: Собрать сайт из одного файла
        run: |
          mkdir -p _site
          cp tray-generator.html _site/index.html
      - uses: actions/configure-pages@v5
        with:
          enablement: true
      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site
      - id: deployment
        uses: actions/deploy-pages@v4
